This folder receives the compiled Angular member SPA.

Build + deploy from repository root:

```bash
cd frontend/member-spa
npm install
npm run build:deploy
```

The deploy script copies files to:

`components/com_balancirk/media/member-spa/browser`
